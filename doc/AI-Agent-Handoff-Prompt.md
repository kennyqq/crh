# 可直接给下一位 AI Agent 的接手提示词

请先阅读以下文件，再开始改动代码：

1. `D:\Codex\crh\doc\AI-Agent-Context-Handoff.md`
2. `D:\Codex\crh\src\App.tsx`
3. `D:\Codex\crh\src\mock\screenData.ts`
4. `D:\Codex\crh\src\features\p1\P1CorridorMap.tsx`
5. `D:\Codex\crh\src\mock\p1CorridorData.ts`
6. `D:\Codex\crh\src\features\p2\P2Safeguard.tsx`
7. `D:\Codex\crh\src\App.css`

你正在接手一个前端展会 Demo，路径是：

- `D:\Codex\crh`

项目技术栈：

- `React + TypeScript + Vite`

当前只保留 3 个页面：

1. `P0 线路态势`
2. `P1 感知洞察`
3. `P2 策略保障`

`P3 投诉回溯` 已从入口删除。

接手时请遵守这些约束：

## 业务约束

- 整个 Demo 的口径必须是：
  - 整体网络良好
  - 局部存在问题
  - 不能让领导一眼看成“全网很差”
- `P1` 是最重要、也最容易改坏的一页
- `P1` 必须优先使用高德地图空间感，不要退回成只有抽象线的 fallback 主呈现
- `P1` 默认展示全部 5 个告警
- 点击地图告警点或右侧问题列表，自动切到该告警对应时间切片
- 时间轴是从属导航，不是主控制器

## 最近用户明确要求但可能还未完全收尾

1. `P1` 左上文案应显示为：
   - `京广高速线 - 对应站点名`
   - 点击哪个告警，就显示哪个站点名
2. `P1` 右侧问题表头里的 `位置` 改成 `站点`
3. `P1` 图例中的热力改为：
   - 名称：`信号电平 RSRP`
   - 三色：`红 / 黄 / 绿`
   - 语义：`差 / 良 / 优`
4. `P1` 线路整体应以绿色居多
5. `P1` 告警点颜色与热力图区分，使用：
   - 深红
   - 橙色
   - 蓝色

## 设计约束

- 不要重新加回大段说明文字
- 不要把讲解词写回页面
- 继续保持“真实运营系统”风格，而不是“PPT 说明页”
- 容器发光和描边要克制

## 你开始动手前要做的事

1. 先确认 `P1` 当前是不是高德地图渲染
2. 再确认 `P1` 的 5 个告警点是否仍可点击
3. 再确认 `P1` 时间轴切换是否仍能联动右侧指标
4. 不要先动 P0/P2，优先把 P1 收稳

## 你完成后要做的事

1. 运行：

```powershell
cd D:\Codex\crh
npm.cmd run build
```

2. 实际打开页面验证：
   - `P0` 可正常显示
   - `P1` 不黑屏
   - `P1` 地图是高德底图
   - `P1` 告警点可点击
   - `P2` 不受影响

3. 不要自动推送到 GitHub，除非用户明确要求。
